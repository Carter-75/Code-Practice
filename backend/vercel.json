{
  "version": 2,
  "builds": [
    {
      "src": "app.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["prompts/**", "routes/**", "models/**", "services/**", "controllers/**"]
      }
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "app.js" }
  ]
}
